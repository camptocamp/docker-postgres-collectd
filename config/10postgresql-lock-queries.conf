<Plugin "postgresql">

  # locks view from pg_locks
  <Query pg_locks>
    Statement "SELECT COUNT(*) AS lock_count FROM pg_locks WHERE NOT GRANTED;"
    <Result>
      Type gauge
      InstancePrefix pg_locks
      ValuesFrom lock_count
    </Result>
  </Query>

</Plugin>
